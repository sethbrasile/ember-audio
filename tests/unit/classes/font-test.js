import { Font } from 'ember-audio';
import { module, test } from 'qunit';

module('Unit | Class | font', function () {
  // Replace this with your real tests.
  test('it exists', function (assert) {
    let result = Font.create();
    assert.ok(result);
  });
});
