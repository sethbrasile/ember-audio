import getPositionFor from 'dummy/utils/get-position-for';
import { module, test } from 'qunit';

module('Unit | Utility | get position for');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = getPositionFor();
  assert.ok(result);
});
