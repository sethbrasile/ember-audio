import utils from 'dummy/utils/utils';
import { module, test } from 'qunit';

module('Unit | Utility | utils');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = utils();
  assert.ok(result);
});
