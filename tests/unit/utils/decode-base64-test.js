import decodeBase64 from 'dummy/utils/decode-base64';
import { module, test } from 'qunit';

module('Unit | Utility | decode base64');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = decodeBase64();
  assert.ok(result);
});
