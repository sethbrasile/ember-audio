import getProp from 'ember-audio/utils/get-prop';
import { module, test } from 'qunit';

module('Unit | Utility | get prop');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = getProp();
  assert.ok(result);
});
