import getDurationFor from 'dummy/utils/get-duration-for';
import { module, test } from 'qunit';

module('Unit | Utility | get duration for');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = getDurationFor();
  assert.ok(result);
});
