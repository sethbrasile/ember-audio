import getPropFromSound from 'dummy/utils/get-prop-from-sound';
import { module, test } from 'qunit';

module('Unit | Utility | get prop from sound');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = getPropFromSound();
  assert.ok(result);
});
